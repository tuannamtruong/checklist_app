android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.caching=true
# Keep the build reproducible in CI: no daemon reuse across container runs.
org.gradle.daemon=false
